
This entire new endpoint need to be triggered after the first scan. Adds extra overhead when changing logic as well as when running the PII job. 

Can it be changed to read an entire row? Then it's just a couple of lines of code? 
Can also alternatively be written in sql.

