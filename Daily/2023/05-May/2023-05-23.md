---
categories:
  - "[[Daily]]"
created: 2026-06-25
---
🛫 - [ ] Run actual staging table
	[x] views are already filtered should materialized be?
	[x] 14:00: increase max run duration
	[x]  Increase the refernce in alp table
	 [] Purge tables and pubsub
	 [] Run staging table creation
	 [] Need to add alp key since it can't be read. 
  
  - [ ] Open - Scanner: For v3: Update field name predector
  - [ ] Create task to keep track of the filtering out float

- [x] Report hours MMS
- [x] [] Give estimate for vacation ✅ 2023-05-23

📅  2023-05-24  [ ] I think that's perfectly fine. I noticed one thing that might want to address also. Right now the processing stamp is the time each table was processed and won't be same for all the tables for the same batch. I think this is something we might want to fix.

Netlight
[x]  Meeetin mentor



Problem: 
#datahub-issue The glossary was not loading. 

Sebastian checked the architecture overviews
Sebastian tried to find in elastich search

lokked at logs in gms 
saw that logs for changes was missing in elasticsearch and ne04j
maybe the consumer or the topic the messages aren't being processed. 
investigated 1-2 days of both elastic and neo4j
Jumped into kafka 

kafka-topics.sh --bootstrap-server=localhost:9092--list
We can see all the topics in kafka

DataHubUpgradeHistory_v1
DataHubUsageEvent_v1
FailedMetadataChangeEvent_v4
FailedMetadataChangeProposal_v1
MetadataAuditEvent_v4
MetadataChangeEvent_ v4
MetadataChangeLog_Timeseries_v1
MetadataChangeLog_Versioned_v1
MetadataChangeProposal_v1
FailedMetadataChangeProposal_v1

We have 3 kafka brokers 


We have replicas 
Leader is who is responsible for the topic
Ledder 0 is original and 1,2 is replicas

If you have more partitions, which we have, one consumer can consume all partitions but it's slow. Kafka solution for parallelism is that one consumer should consume from one partition.
Right now we have 50 consumers (to one partition?).
50 partitions and 50 consumers, 1 consumer per partition. 
if you have 1 partition and 50 consumers, 1 can consume and 49 will be queued. 

It was only 1 consumer from the beginning. 

ISR: in sync replica. 
In sync means that all the information was copied for the replicas. 
If the leaders goes offline kafka decide who will be the next leader and consume from broker. 


current offset - what consumer is at the moment  
log end offset - current offset of partition
lag - log end offset - current offset

Our lag number was really high 

To reduce the LAG Sebastian increased the partitions. 

If new consumer is joining to the group kafka start new assignment to consumers. Kafka remove all the consumers for the topic and after that try to assign again the consumer to the group. But it the assignment is not done it starts assigning again.  New pod was created, new consumer want to join and a loop was created. 

**Demo issue with loop that was created with new pods making new consumer rebalancing.** 

We can see that our connection is stable wit 50 consumers
Then we deleted pod then we have stable 40. 
Preparing rebalance 50

![[Pasted image 20230523103622.png]]



we can see in the logs in prerequisites-kafka
TODO: add picture

Added delay KAFKA_CFG_GROUP_INITIAL_REBALANCE_DELAY_MS. Inspiration from udemy kafka setup.  
![[Pasted image 20230523103944.png]]

If new pods are starting all the consumers goes online at the same time. Reduce signal for rebalancing it's better to add delay and the pods can start and you only get one signal. 

![[Pasted image 20230523104048.png]]

Increase NFG_NUM_NETWORK_THREADS

![[Pasted image 20230523104224.png]]
Spring_KAFKA_CONSUMER_MAXPOLLRECORDS - how many messages it pulls. Reduced to 20 from 500.  
increase CONSUMER_PROPERTIES_MAX_POLL_INTERVALL_MS since some message was taking too long,.

These are also needed to add if standalone_consumers is set to true.
datahub-mce-consumer replicaCount can only be 1. 

Kafka partitions is set to 50. 
ReplicationFactor is 3. 

We ran first without replicationfactor
we  had partitino 1 first but changed that to 50. 


split the consumers to its own processes and we get a stable connection and 50 consumers. All the consumer groups has the same. 

The group is idle 
![[Pasted image 20230523102238.png]]

Here we can see that we have a lag of 5. 
![[Pasted image 20230523103111.png]]

We had lag about 3000

We can increase scalability by increasing partition (and or consumers)
We need to have 80-100 partitoins 


Why is the consumer slow? 
The dataset provide also the lineage and some dataset with large relation to another entitiy need some time to process need some time to process. Can take 15 min. Ingestion of data owners are very fast but dataset are heavier. 


### NEO4j 
Increase the cpu and memory

Added own config for neo4j 
![[Pasted image 20230523105536.png]]

To increase pagecache.size to 50G
It is recommended to increase pagecache you avoid the IO access.
We're using only community version so we only get one instance. Enterprise can get more instances to balance traffic.  
Kafka is using bitmami helm chart and other companies have own broker and setup.
Lot of people are using kafka confluent which provide some powerful kafka cluster. 

### Elasticsearch 



Kafka bottleneck for updates 

Lineage relationship 

### Trailing commas 

It's used because it's producing a smaller git diff. You can see some discussion here
https://stackoverflow.com/questions/12087742/should-i-add-a-trailing-comma-after-the-last-argument-in-a-function-call
Can se guidance on how to use trailing commas here for PEP8: 
https://peps.python.org/pep-0008/#when-to-use-trailing-commas
k



Distract reader 
Clutters PR - goes both ways 
Change to one file becomes a massive change