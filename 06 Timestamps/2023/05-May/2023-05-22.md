- Finish datahub writing - **Waiting for review** 
	[x] Rename glossary_to_dataset to glossary_term_to_dataset
	[x] might need to fetch the environment
[x] Read Thorsten's PR - # fix(imports for trf) 
[x] Read about the temp file vulnerability for the transformer models

Netlight
- [ ] Schedule meeting with datahub orga ⏳ 2023-05-22📅



Datahub writing type hinting problem 
Getting this problem from pyright

```bash
 /Users/hugo/Work/anase-datahub-push-ingestion/worker/app/ingestion/emitters/glossary_to_dataset.py:79:41 - error: Cannot access member "field_name" for type "Dataset"
 ```
This is since the props have different parameters (field_name doesn't exist for Dataset).  One solution according to ChatGPT could be to make a Props class but this would mean we can't access worker_event.props but need to have worker_event.dataset.props which maybe is better though. 
```python
class Props(BaseModel):
    dataset: Optional[Dataset]
    domain: Optional[Domain]
    table_owners_to_dataset: Optional[TableOwnersToDataset]
    domain_to_dataset: Optional[DomainToDataset]
    glossary_term_to_dataset: Optional[GlossaryTermToDataset]
```

